Question:
Given an integer x, return true if x is a palindrome, and false otherwise.

Intuition
First I consider the reverse method but it can not use in interger,so I change mine thinking .

Approach
I turn interger into string and modify the sequence to achieve the targe

Code:

class Solution:
    def isPalindrome(self, x: int) -> bool:
        if str(x)==str(x)[::-1]:
            return True
        else:
            return False
s = Solution()
result = s.isPalindrome(121)
print(result)
